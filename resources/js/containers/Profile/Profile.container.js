import React from 'react';
import ProfileComponent from './Profile.component';

const Profile = (props) => { 
    return(
        <div>
            <ProfileComponent />
        </div>
    );
};

export default Profile;
